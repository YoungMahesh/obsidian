### Enable zoom on obsidian start

Settings → Community Plugins → QuickAdd (plugin) v2.23.0 → settings → New Choice → Macro 
→ Repeat 3 times:  (Add wait command (Top-row.clock-icon) + Obsidian Command -> zoom in -> Add) 
→ Run on Startup - on 
→ Done